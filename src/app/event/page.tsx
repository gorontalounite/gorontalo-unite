import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Event Gorontalo | Gorontalo Unite", description: "Agenda dan event yang berlangsung di Gorontalo." };
const format = (date: string) => new Intl.DateTimeFormat("id-ID", { dateStyle: "medium", timeStyle: "short", timeZone: "Asia/Makassar" }).format(new Date(date));
export default async function EventPage() { const supabase = await createClient(); const { data: events } = await supabase.from("events").select("id,title,slug,description,image_url,venue,starts_at,ends_at,featured").eq("published", true).gte("starts_at", new Date().toISOString()).order("starts_at").order("featured", { ascending: false }); return <main className="min-h-screen bg-gray-950 pb-20 text-white"><section className="mx-auto max-w-7xl px-4 pt-12 sm:px-6 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[.24em] text-amber-300">City Guide</p><div className="mt-3 flex flex-col gap-4 border-b border-white/10 pb-10 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="font-heading text-4xl font-bold sm:text-6xl">Event Gorontalo</h1><p className="mt-3 max-w-2xl text-gray-400">Agenda pilihan, detail acara, lokasi, dan tautan pendaftaran dalam satu tempat.</p></div><Link href="/" className="text-sm text-gray-400 hover:text-amber-300">← Kembali ke beranda</Link></div></section><section className="mx-auto max-w-7xl px-4 pt-8 sm:px-6 lg:px-8">{!events?.length ? <div className="rounded-3xl border border-dashed border-white/20 px-6 py-24 text-center"><h2 className="font-heading text-2xl font-semibold">Belum ada event mendatang</h2><p className="mt-2 text-sm text-gray-400">Agenda Gorontalo akan ditambahkan di sini.</p></div> : <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{events.map((event) => <Link key={event.id} href={`/event/${event.slug}`} className="group overflow-hidden rounded-3xl bg-zinc-900 ring-1 ring-white/10 transition hover:-translate-y-1 hover:ring-amber-300"><div className="relative aspect-[4/3] bg-gradient-to-br from-amber-500 to-orange-700">{event.image_url && <Image src={event.image_url} alt={event.title} fill unoptimized className="object-cover transition duration-500 group-hover:scale-105" />}</div><div className="p-5"><p className="text-xs font-semibold uppercase tracking-wide text-amber-300">{format(event.starts_at)}</p><h2 className="font-heading mt-2 text-xl font-semibold">{event.title}</h2>{event.venue && <p className="mt-2 text-sm text-gray-400">📍 {event.venue}</p>}<p className="mt-3 line-clamp-2 text-sm leading-relaxed text-gray-400">{event.description}</p></div></Link>)}</div>}</section></main>; }
