import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Event Gorontalo | Gorontalo Unite", description: "Agenda dan event yang berlangsung di Gorontalo." };
const format = (date: string) => new Intl.DateTimeFormat("id-ID", { dateStyle: "medium", timeStyle: "short", timeZone: "Asia/Makassar" }).format(new Date(date));
export default async function EventPage() { const supabase = await createClient(); const { data: events } = await supabase.from("events").select("id,title,slug,description,image_url,venue,starts_at,ends_at,featured").eq("published", true).gte("starts_at", new Date().toISOString()).order("starts_at").order("featured", { ascending: false }); return <main className="min-h-screen bg-stone-950 pb-20 text-white"><section className="mx-auto max-w-7xl px-4 pt-16 sm:px-6 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[.24em] text-[#f5c400]">Agenda Gorontalo</p><div className="mt-3 flex flex-col gap-4 border-b border-white/15 pb-10 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="font-display text-5xl font-bold tracking-[-.05em] sm:text-7xl">Event</h1><p className="mt-3 max-w-2xl text-stone-300">Agenda pilihan, detail acara, lokasi, dan tautan pendaftaran dalam satu tempat.</p></div><Link href="/" className="text-sm text-stone-400 hover:text-[#f5c400]">← Kembali ke beranda</Link></div></section><section className="mx-auto max-w-7xl px-4 pt-10 sm:px-6 lg:px-8">{!events?.length ? <div className="border border-dashed border-white/20 px-6 py-24 text-center"><h2 className="font-display text-2xl font-semibold">Belum ada event mendatang</h2><p className="mt-2 text-sm text-stone-400">Agenda Gorontalo akan ditambahkan di sini.</p></div> : <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{events.map((event) => <Link key={event.id} href={`/event/${event.slug}`} className="group overflow-hidden rounded-[.65rem] border border-white/15 bg-white/5 transition hover:-translate-y-1 hover:border-[#f5c400]"><div className="relative aspect-[4/3] bg-stone-800">{event.image_url && <Image src={event.image_url} alt={event.title} fill unoptimized className="object-cover transition duration-500 group-hover:scale-105" />}</div><div className="p-5"><p className="text-xs font-semibold uppercase tracking-wide text-[#f5c400]">{format(event.starts_at)}</p><h2 className="mt-2 text-xl font-semibold">{event.title}</h2>{event.venue && <p className="mt-2 text-sm text-stone-400">📍 {event.venue}</p>}<p className="mt-3 line-clamp-2 text-sm leading-relaxed text-stone-400">{event.description}</p></div></Link>)}</div>}</section></main>; }
