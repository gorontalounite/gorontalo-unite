import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Event Gorontalo | Gorontalo Unite", description: "Agenda dan event yang berlangsung di Gorontalo." };
const format = (date: string) => new Intl.DateTimeFormat("id-ID", { dateStyle: "medium", timeStyle: "short", timeZone: "Asia/Makassar" }).format(new Date(date));
export default async function EventPage() { const supabase = await createClient(); const { data: events } = await supabase.from("events").select("id,title,slug,description,image_url,venue,starts_at,ends_at,featured").eq("published", true).gte("starts_at", new Date().toISOString()).order("starts_at").order("featured", { ascending: false }); return <main className="min-h-screen bg-black pb-24 pt-28 text-white md:pt-36"><section className="mx-auto max-w-[1500px] px-5 md:px-10 lg:px-16"><p className="text-xs font-semibold uppercase tracking-[.24em] text-[#f4c300]">Kalender Gorontalo</p><div className="mt-3 flex flex-col gap-4 border-b border-white/20 pb-10 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="font-display text-[16vw] font-semibold uppercase leading-[.75] tracking-[-.07em] md:text-[8vw]">Event</h1><p className="mt-6 max-w-md text-base leading-relaxed text-white/60">Agenda pilihan, detail acara, lokasi, dan tautan pendaftaran dalam satu tempat.</p></div><Link href="/" className="text-sm text-white/50 hover:text-[#f4c300]">← Kembali ke beranda</Link></div></section><section className="mx-auto max-w-[1500px] px-5 pt-10 md:px-10 lg:px-16">{!events?.length ? <div className="border border-dashed border-white/20 px-6 py-24 text-center"><h2 className="font-display text-2xl font-semibold">Belum ada event mendatang</h2><p className="mt-2 text-sm text-gray-400">Agenda Gorontalo akan ditambahkan di sini.</p></div> : <div className="grid gap-3 md:grid-cols-2 lg:grid-cols-3">{events.map((event) => <Link key={event.id} href={`/event/${event.slug}`} className="group overflow-hidden bg-zinc-900 ring-1 ring-white/10 transition hover:-translate-y-1 hover:ring-[#f4c300]"><div className="relative aspect-[4/3] bg-gradient-to-br from-amber-500 to-orange-700">{event.image_url && <Image src={event.image_url} alt={event.title} fill unoptimized className="object-cover transition duration-500 group-hover:scale-105" />}</div><div className="p-5"><p className="text-xs font-semibold uppercase tracking-wide text-[#f4c300]">{format(event.starts_at)}</p><h2 className="mt-2 font-display text-2xl font-semibold uppercase leading-none">{event.title}</h2>{event.venue && <p className="mt-3 text-sm text-white/50">📍 {event.venue}</p>}<p className="mt-3 line-clamp-2 text-sm leading-relaxed text-white/55">{event.description}</p></div></Link>)}</div>}</section></main>; }
